spring:
  application:
    name: brief20
  datasource:
    url: jdbc:postgresql://localhost:5432/brief20
    username: postgres
    password: postgres
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update